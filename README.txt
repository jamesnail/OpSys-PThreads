To run, just compile it using 'make all', then when executing,
you can provide -x number and -y number as arguments for x and y,
those being the number of threads and iterations per thread, respectively.